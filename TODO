#todo
  Use Github secrets to use the discord token
  Multi-platform tts bot
  Use git API to create a command that replies with the current version it's using/the commit message of that version
  Verification so we can see who let someone in
  Revise
  *crypto?????*

#in beta


#done
